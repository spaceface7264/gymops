dist
coverage
node_modules
package-lock.json
src/lib/database.types.ts
PROJECT_SPEC.md
PROJECT_TASKS.md
PROJECT_STATE.md
CLAUDE.md
supabase/.temp
# Deno formats the Edge Functions (`deno fmt`), the way ESLint already skips them
supabase/functions

# Rust build output and the schemas Tauri regenerates on every build (P7-01)
src-tauri/target
src-tauri/gen

# SDD scratch, never committed
.superpowers
